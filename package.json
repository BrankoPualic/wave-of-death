{
  "name": "wave-of-death",
  "version": "1.0.0",
  "description": "MVP",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "live-server",
    "start": "start tsc --watch && start live-server"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
